name: Deploy Function App

on:
  workflow_dispatch:
    inputs:
      slot-name:
        description: |
          Azure deployment slot:
          - Leave empty → deploy to Production
          - Enter "staging", "test", or any custom slot
          - Approval is required for all deployments
        required: false
        default: ""
      environment:
        description: GitHub Environment for approval
        required: true
        type: choice
        options:
          - app1-test
          - app1-prod
          - app2-test
          - app2-prod

jobs:
  deploy-function-app:
    uses: ./.github/workflows/reusable-workflow.yml
    with:
      function-app-name: test-function-0392
      resource-group: test-rg
      slot-name: ${{ github.event.inputs.slot-name }}
      environment: ${{ github.event.inputs.environment }}
      working-directory: functionapp/functions
      package-path: functions/build.zip
      node-version: 18.x
    secrets: inherit
